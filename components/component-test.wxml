<!--components/component-test.wxml-->
<text>components/component-test.wxml</text>
<view class="inner" bindtap='componentTap'>
  {{innerText}}---{{testData}}

    <!-- 这部分内容将被放置在组件 <slot name="after"> 的位置上 -->
    <view slot="after">这里是插入到组件slot name="after"中的内容</view>
</view>